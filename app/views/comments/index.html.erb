<p style="color: green"><%= notice %></p>

<h1>Comments</h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <%= render comment %>
    <p>
      <%#= link_to "Show this comment", @comment %>
    </p>
  <% end %>
</div>

<%= link_to "Go back to the article", article_path(@article) %>
